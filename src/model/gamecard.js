export default class GameCard {

  constructor(difficulty, question, answer, choices) {
    this.difficulty = difficulty;
    this.question = question;
    this.answer = answer;
    this.choices = choices;
  }
  
}